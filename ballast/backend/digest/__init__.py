"""Digest — the opt-in calm weekly email (Story 5.1, FR21).

The sole owner of digest concerns (AD-6): a per-user opt-in, an EmailPort behind
a factory (AD-8), static calm-voice composition, and a SYSTEM-scope idempotent
send job. Email is the only channel — never push, never SMS.
"""
