# Deferred Work

Incidental, real issues surfaced during autonomous review passes but out of scope for the story that found them. Each entry is appended by the dev-auto review step; do not edit existing entries.

- source_spec: `_bmad-output/implementation-artifacts/3-1-market-data-ingestion-market-daily.md`
  summary: Harden the TiingoAdapter response parser against real-vendor payload shapes (missing/None OHLC fields, non-list error payloads, robust ISO-8601 timezone→calendar-day handling) when real Tiingo network wiring lands.
  evidence: The adapter's `fetch_eod` assumes well-formed rows (`row["date"][:10]`, `Decimal(str(row[field]))`, `row.get("volume")`); a missing/null field or a non-list payload would raise inside the per-symbol try/except and silently skip that symbol. Deliberately out of scope for 3.1 (credential-gated stub, "do NOT wire real network here"), but it is real code that will run once `MARKETDATA_ADAPTER=tiingo` with a live key — revisit alongside the first real Tiingo integration.

- source_spec: `_bmad-output/implementation-artifacts/3-2-drawdown-matching-evidence-record-contract.md`
  summary: The Precedent Engine's recovery-day and 252-"trading-day" forward-return math offset by ROW INDEX, so both stats silently assume `market_daily` holds exactly one contiguous, gapless row per trading day; the engine neither owns nor verifies that invariant.
  evidence: `precedent/engine.py` computes `fwd_index = trough_index + FORWARD_RETURN_DAYS` and `recovery_days = recovery_index - trough_index` on row positions. If ingestion (Story 3.1 / a real Tiingo feed) ever leaves a gap or inserts a non-trading-day row, "252 trading days" spans the wrong horizon and recovery counts drift. Out of scope for 3.2 (fake/crafted data is dense by construction); revisit when real Tiingo history lands (alongside the vendor-hardening item above), e.g. a contiguity assertion at ingest or a calendar-aware offset in the engine.

- source_spec: `_bmad-output/implementation-artifacts/3-2-drawdown-matching-evidence-record-contract.md`
  summary: The Precedent Engine does not validate that `adj_close` values are positive; degenerate zero/negative vendor data could produce a drawdown magnitude > 100% (division guards prevent a crash but not a nonsensical stat).
  evidence: `historical_episodes`/`current_drawdown` guard `peak_close > 0` and `base > 0` against divide-by-zero, but a positive peak with a non-positive trough yields `magnitude = (peak - trough)/peak > 1`, which would flow into band-matching and a "~>100% below peak" statement. Cannot occur with the deterministic fake/crafted series (all positive); real bad-data handling belongs with the Tiingo vendor-hardening pass. Consider skipping/flagging non-positive `adj_close` in `_load_series` or clamping magnitude to ≤ 1 then.

- source_spec: `_bmad-output/implementation-artifacts/3-2-drawdown-matching-evidence-record-contract.md`
  summary: The Precedent Engine's ±2.5pp magnitude band has no lower floor, so a trivially small historical wiggle can qualify as a cited "precedent" for a small current drop.
  evidence: `_match_and_rank` filters episodes by `abs(episode.magnitude - current_magnitude) <= MAGNITUDE_BAND` (0.025) with no minimum episode magnitude. A ~0.1–0.5% historical dip that still forms a peak→trough→recovery episode falls in the band around a small current drawdown and is surfaced as an `event-precedent` window — a statistically meaningless "precedent" that would read as noise to a beginner. Cannot surface with the deterministic crafted test series (episodes are clean 8% drops); revisit when tuning band parameters against real Tiingo history (e.g. add a minimum-episode-magnitude floor or a relative band).

- source_spec: `_bmad-output/implementation-artifacts/3-4-missed-growth-meter.md`
  summary: The missed-growth meter reports "no idle cash" for all-cash accounts (the AD-14 cash-only cache gap), so the users with the MOST idle cash — the meter's primary audience — see a confidently false "you have no idle cash" message; close alongside the AD-14 real-Schwab-balances mapping.
  evidence: `estimate_missed_growth` reads idle cash via `get_portfolio(scope, session).cash`, which derives cash from a representative `portfolio_cache` holdings row. A cash-only account (cash > 0, zero holdings) yields NO cache rows, so cash surfaces as `Decimal("0")` and the endpoint returns `reason:"no_idle_cash"` with the copy "You have no idle cash sitting out of the market right now." This is the pre-existing deferred AD-14 cash-only gap (holdings-shaped cache has no dedicated balances row) — Story 3.4 explicitly scoped it out and reads cash as the cache reports it. The meter benefits automatically once AD-14's balances mapping lands upstream; no engine/view change is needed here.

- source_spec: `_bmad-output/implementation-artifacts/3-5-headline-contextualizer.md`
  summary: The shared `directionAndMagnitude` helper (in `frontend/src/lib/precedent.js`, from Story 3.3) paints an EXACTLY-zero return as a green ▲ `+0.0%` gain rather than a neutral non-event, a mild color-honesty gap now surfaced through a second calming view (the headline contextualizer) as well as the recovery view.
  evidence: `direction: n < 0 ? 'down' : 'up'` maps `0` to `'up'`, so a forward-return median or per-window forward return of exactly `"0.0000"` renders as a green ▲ "+0.0% … return" — technically a non-gain shown as a gain. Low probability with real decimal medians but reachable. Deliberately not fixed under 3.5 because the helper is shared Story-3.3 code and changing its zero handling would alter the recovery view's rendering too (out of scope for this story); revisit as a focused honesty pass across both views (mirror Story 3.4's flat-window "neutral, not a gain" treatment for the missed-growth dollar figure).

- source_spec: `_bmad-output/implementation-artifacts/4-1-llm-gateway.md`
  summary: The real AnthropicGateway.complete() path has no runtime hardening — it does not inspect `stop_reason`, guards neither the `next(text-block)` StopIteration nor `json.loads` on non-JSON output, does not wrap `anthropic.APIError`/timeout/rate-limit into a typed gateway error, does not validate messages are non-empty/role-shaped, and always uses non-streaming `messages.create` (SDK 400/ValueError for `max_tokens` >~16K).
  evidence: In `llm/anthropic_adapter.py`, a refusal or `max_tokens` truncation makes the first text block invalid JSON so `json.loads` raises a raw `JSONDecodeError`; an all-thinking/tool_use response raises bare `StopIteration`; vendor exceptions leak past the port, coupling callers to the `anthropic` SDK and undermining the AD-6 abstraction. All of this is in the credential-gated real path that Story 4.1 deliberately scoped as a thin transport and does NOT exercise (fake-first, zero-network tests; "no streaming in v1"). Harden it when 4.2/4.3 first flip `LLM_ADAPTER=anthropic` with real integration context: check `stop_reason`, wrap `json.loads`/`StopIteration`/`anthropic.APIError` into a typed `LLMError`, validate messages, and route large-`max_tokens` calls through `client.messages.stream().get_final_message()`.

- source_spec: `_bmad-output/implementation-artifacts/4-1-llm-gateway.md`
  summary: The FakeLLMGateway's deterministic JSON-Schema instance generator (`_placeholder`) handles only object/array/string/integer/number/boolean/null and honors `enum` only inside the string branch; it ignores `anyOf`/`oneOf`/`allOf`/`$ref`, non-string `enum`, and numeric/length/`minItems` constraints, and would recurse infinitely on a self-referential schema.
  evidence: In `llm/fake_adapter.py`, `_placeholder` returns a `fake-<field>` string for composed/`$ref` nodes and for objects declared only via composition, ignores integer/boolean `enum` (returns `_seed % 100`), and has no cycle guard (RecursionError on recursive schemas). None of these occur for the v1 coach's Recommendation schema (flat objects/strings/arrays/string-enums), and the real Anthropic structured-outputs API itself rejects recursive schemas and numeric/length constraints, so the fake diverging is low-consequence today. Revisit when a coach story needs one of these schema shapes: move the `enum` check ahead of type dispatch, add composition/`$ref` resolution, and add a depth/seen-set cycle guard.

- source_spec: `_bmad-output/implementation-artifacts/4-6-propose-and-approve-execution.md`
  summary: The live-session execution gate (`require_live_broker_session`) authorizes an order on `expires_at` alone and never checks that the session token's `provider` matches the broker adapter selected by `BROKER_ADAPTER`, so a user with a live `provider="fake"` token could execute against a real `schwab` adapter (or vice-versa) — the session judged "live" and the adapter that actually places the order are decoupled.
  evidence: `api/deps.py::require_live_broker_session` returns on `broker_session.is_live` (computed purely from `expires_at` in `brokers/session.py`); `brokers/factory.py::get_broker()` selects the adapter from config independently of the token row, and the new `api/coach.py::approve` wires the two together without asserting `broker_session.provider == broker.provider`. Fully masked in Story 4.6 (everything is the fake adapter, zero network), and provider↔placement consistency is squarely the Story 4.8 "approval→placement integrity" concern — revisit there, asserting provider agreement before placing (or re-establishing the correct session).

- source_spec: `_bmad-output/implementation-artifacts/4-7-order-outcomes-reconciliation.md`
  summary: Money-string serialization can emit exponent notation — `api/coach.py::_to_approve_response` serializes `filled_qty` (and `/recommend` serializes `amount`) via bare `str(Decimal)`, so an absurdly large order amount (~1E29) surfaces `"1E+27"` on the wire, technically violating the "amounts serialized as plain decimal strings" convention.
  evidence: `_to_approve_response` does `filled_qty=str(outcome.filled_qty)`; the fake computes `filled_qty = amount / FAKE_FILL_PRICE`, and `Decimal("1E29")/Decimal("100.00")` stringifies to `"1E+27"` (verified). Pre-existing from Story 4.6 (the `str(...)` line is unchanged by 4.7) and the value round-trips correctly through the documented `Decimal(str(...))` consumer, so every test passes; amount magnitude is intentionally unbounded (4.6 rejected a size cap as contradicting FR11 "never block"). Reachable only at unrealistic magnitudes. Fix in a focused money-formatting pass: serialize with a fixed-point formatter, e.g. `format(value, "f")`, across `filled_qty`/`avg_price`/`amount` so the wire never carries `E+` notation.

- source_spec: `_bmad-output/implementation-artifacts/4-9-co-signed-immutable-decision-record.md`
  summary: The co-signed-decision idempotency guard is not atomic against two simultaneous in-flight `/approve` calls carrying the same `decision_id` — read-status → place_order → write-cosign is not one transaction, the idempotency key is minted per-request (not a stable persisted per-decision key), and there is no `SELECT … FOR UPDATE` / conditional `WHERE status='proposed'` update / unique index — so concurrent approves could both pass the "not cosigned" check and place twice.
  evidence: `api/coach.py::approve` loads the record via `load_decision` (a plain scoped `SELECT`, no row lock), checks `record.status == "cosigned"`, then `await execute_approved_order(...)` (yielding the event loop) before `cosign` + `session.commit()`; two overlapping requests both observe `proposed`, each mints its own key (`mint_idempotency_key()` at request time), and each calls `broker.place_order`. The persisted-key defense only kicks in AFTER the first commit, so it does not cover the in-flight window; the existing `test_double_approve_is_idempotent_no_double_place` is strictly sequential. No consequence in v1 (the Schwab adapter is a credential-gated stub — no real order can be placed; single-user dev), and a correct fix needs `ScopedRepository` locking / a two-phase proposed→cosigning→cosigned claim plus a stable per-decision key and broker-side key dedup. Revisit when the real Schwab `place_order` is wired (where a double-place moves real money): claim the decision atomically before placing, persist a stable idempotency key at proposal time reused across placements, and add a DB unique constraint on `idempotency_key`.

- source_spec: `_bmad-output/implementation-artifacts/4-9-co-signed-immutable-decision-record.md`
  summary: Every `/api/coach/recommend` writes one immutable `decision_record` row and nothing prunes the never-co-signed `proposed` proposals, so the table grows without bound per user (the vast majority of recommendations are never approved).
  evidence: `api/coach.py::recommend` unconditionally calls `record_proposal(...)` + `session.commit()` on each call; the table is insert-only with no TTL/retention and only the mixin's `owner_id` index (no `(owner_id, created_at)` index for the eventual replay/history query in 4.10). Intended design for the "on-the-record memory," and harmless at current scale, but a real retention/cost concern at volume. Revisit alongside Story 4.10 (Decisions history & replay), e.g. prune/expire stale un-cosigned proposals and add a composite index for the history listing.

- source_spec: `_bmad-output/implementation-artifacts/5-1-opt-in-calm-weekly-email-digest.md`
  summary: The digest job's ISO-week idempotency guard is a non-atomic read-check-then-write (`if pref.last_sent_week == week_key` … send … `UPDATE`), with no row lock, no conditional `WHERE last_sent_week IS DISTINCT FROM :week`, and a unique index only on `owner_id` — so two overlapping/retried job runs can both observe the stale marker and both send, double-emailing a user.
  evidence: `digest/job.py::send_weekly_digests` reads the snapshotted `last_sent_week` then later commits the marker in a separate step; the module docstring claims "an overlapping run never double-sends," but only strictly-sequential re-runs are actually guarded (and tested). A duplicate proactive email directly violates the product's core "one sanctioned proactive touch / never nag" invariant (NFR8). Deployment is a single weekly cron so concurrent overlap is unlikely today; revisit if the job is ever parallelized or aggressively retried — make the marker advance a conditional `UPDATE … WHERE last_sent_week IS DISTINCT FROM :week` and gate the send on `rowcount == 1`, or `SELECT … FOR UPDATE` the pref row.

- source_spec: `_bmad-output/implementation-artifacts/5-1-opt-in-calm-weekly-email-digest.md`
  summary: The in-email-body unsubscribe link is a state-changing GET (`GET /api/digest/unsubscribe?token=…`), so a corporate mail-security appliance or link-preview crawler that pre-fetches body URLs can silently opt a user out without any human click.
  evidence: `api/digest.py` `GET /unsubscribe` mutates state (sets `opted_in=False`, commits) and `digest/compose.py::_html_body` embeds that exact GET URL as the clickable `<a href>`. A POST endpoint + `List-Unsubscribe`/`List-Unsubscribe-Post` were added for scanner-safe one-click, but that only covers RFC 8058-compliant clients; the human-visible body link is still the prefetchable GET. The spec's I/O matrix explicitly specifies GET unsubscribe as state-changing, so this is a deliberate design choice, and the failure direction is benign/reversible (user stops receiving email, can re-opt-in from Settings). Revisit as a product decision: make the GET render a confirmation page with a POST form/button so body-link prefetch is harmless while keeping mail-client one-click.

- source_spec: `_bmad-output/implementation-artifacts/5-1-opt-in-calm-weekly-email-digest.md`
  summary: The `last_sent_week` idempotency marker is keyed on the machine's UTC ISO week (`datetime.now(timezone.utc).isocalendar()`), so a scheduler that fires near the UTC Sunday/Monday boundary can land two runs a few hours apart in different UTC weeks (double-send) or drift a run past a boundary (skip a week).
  evidence: `digest/job.py::iso_week_key` derives the key from a UTC datetime; idempotency is only as robust as the scheduler's alignment to the UTC week, which is undocumented. Scheduling is explicitly a deferred deployment concern in this story, and a double-send violates the "one proactive touch" invariant. Revisit when the weekly trigger is wired: document that the cron must fire well inside a UTC week, or key the marker on a send-cadence interval ("no send within N days") that is robust to scheduler drift.

- source_spec: `_bmad-output/implementation-artifacts/5-1-opt-in-calm-weekly-email-digest.md`
  summary: The calm-tone acceptance test is a forbidden-word blocklist (a smoke test, not a tone guarantee), and the unauthenticated unsubscribe confirmation HTML (`_UNSUBSCRIBE_HTML` in `api/digest.py`) is never run through the `_assert_calm` check even though tone is a testable acceptance criterion (NFR8).
  evidence: `tests/test_digest_compose.py::_assert_calm` blocklists ~19 alarmist words with word-boundary matching, which passes plenty of non-calm phrasings ("sell everything", "you're behind") and only guards the composed digest body — not the unsubscribe page copy. Low consequence (copy is static and currently calm), but the guard is weaker than the "tone is testable" AC implies. Revisit in a test-hardening pass: run the unsubscribe copy through `_assert_calm`, and add positive assertions for expected reassuring phrases alongside the negative blocklist.

- source_spec: `_bmad-output/implementation-artifacts/5-1-opt-in-calm-weekly-email-digest.md`
  summary: `DIGEST_FROM_ADDRESS` defaults to a non-empty `ballast@localhost`, so the `SmtpEmailAdapter` credential gate (`if not self._host or not self._from_address`) effectively only trips on an empty `SMTP_HOST` — an operator who sets `EMAIL_ADAPTER=smtp` + `SMTP_HOST` but forgets the from-address will silently send as `ballast@localhost` (rejected/spam-filed by most relays) instead of failing loud.
  evidence: `api/config.py:100` defaults `DIGEST_FROM_ADDRESS = "ballast@localhost"`; `digest/smtp_adapter.py::_require_configured` only raises when host or from-address is empty. The spec's fail-loud intent ("Real adapter, no creds → raises clear EmailNotConfiguredError") is half-disarmed by the non-empty default. The real SMTP adapter is gated and not the tested/shipped path, so no current impact. Revisit when the SMTP adapter is turned on: default `DIGEST_FROM_ADDRESS` to empty so an explicit from-address is required before any real send.

- source_spec: `_bmad-output/implementation-artifacts/5-1-opt-in-calm-weekly-email-digest.md`
  summary: On non-465 SMTP ports the adapter only issues STARTTLS when the server advertises it (`if smtp.has_extn("starttls")`) and then calls `smtp.login()` and sends regardless, so against a relay that doesn't advertise STARTTLS (misconfig or an active STARTTLS-stripping MITM) the SMTP username/password and message go out in cleartext.
  evidence: `digest/smtp_adapter.py` (non-465 branch) treats STARTTLS as best-effort and logs in unconditionally afterward — a classic STARTTLS-downgrade credential-exposure path. The real SMTP adapter is credential-gated and not the tested/shipped path, so no current exposure. Revisit when the SMTP adapter is enabled: make TLS mandatory by default (refuse to `login()` over an unencrypted channel unless the port is implicit-TLS), with an explicit opt-out config for the rare plaintext-relay case.

- source_spec: `_bmad-output/implementation-artifacts/6-1-atomic-decision-claim-idempotency-hardening.md`
  summary: A `proposed` decision_record carried over from before Story 6.1 (or created by any path that doesn't stamp the key) has `idempotency_key IS NULL`, so the winner of the claim reads `None`, `execute_approved_order` silently mints a FRESH key, the order places, and then `cosign`'s equality assertion fires AFTER placement — stranding the record in `cosigning` with a real fill and no co-sign record.
  evidence: `api/coach.py::approve` does `key = record.idempotency_key` (nullable column) then places; `coach/execution.py::execute_approved_order` falls back to `mint_idempotency_key()` when the key is None; `coach/decision_record.py::cosign` asserts the placed key equals the persisted key, so a None persisted key + a minted placement key raises post-placement. Cannot trigger via the normal v1 flow (every new proposed row is keyed at `record_proposal`, and there is no pre-6.1 production data pre-launch), which is why it is deferred rather than patched. Revisit before go-live / Story 6.3 (live Schwab): add a pre-flight guard that refuses to place a decision whose persisted `idempotency_key` is None (or backfill NULL keys), converting a post-fill crash into a pre-fill refusal.

- source_spec: `_bmad-output/implementation-artifacts/6-1-atomic-decision-claim-idempotency-hardening.md`
  summary: The digest claim uses claim-execute → send → commit, so a `session.commit()` failure AFTER `sender.send()` already succeeded rolls the claim back (un-marks the user), and the next run re-sends — a sequential commit-failure double-send that re-opens the NFR8 never-nag guarantee the concurrent claim closes.
  evidence: `digest/job.py::send_weekly_digests` sends then commits the claim; the blanket `except Exception` treats a post-send commit failure the same as a send failure (rollback → user reported failed → re-sent next run). The concurrent in-flight double-send IS closed (the uncommitted conditional UPDATE holds the row lock so a racing run blocks then sees rowcount==0); this residual is only the rare send-succeeds-then-commit-fails window, inherent to send-then-commit without an idempotent send. No current impact (the digest sender is a stub, cron is single-path). Revisit at real-email wiring: make the send idempotent (dedup key on the email side) or move to a send-token the next run can reconcile, so a commit failure after a successful send cannot duplicate the email.

- source_spec: `_bmad-output/implementation-artifacts/6-2-live-llm-gateway-enablement-hardening.md`
  summary: The real `AnthropicGateway.complete()` constructs a fresh `anthropic.Anthropic(api_key=...)` client per call with NO request timeout or `max_retries` configured, so under `LLM_ADAPTER=anthropic` the go-live path forfeits HTTP-connection reuse and a hung Anthropic call blocks `/recommend` for up to the SDK's ~10-minute default timeout before degrading to the default plan.
  evidence: `llm/anthropic_adapter.py::complete` does `client = anthropic.Anthropic(api_key=self._api_key)` on every invocation (unchanged since Story 4.1) and passes no `timeout=`/`max_retries=`; the SDK default request timeout is 10 minutes. Story 6.2's typed-error hardening degrades an APIError (incl. `APITimeoutError`) cleanly, but only AFTER the SDK's long timeout elapses — a 10-minute stall is a practical dead-end for an interactive user even though it is technically "never a dead-end". Picking the right timeout is a product/ops decision (Opus hard-reasoning calls can legitimately take a while, so too-short a timeout would spuriously downgrade a real recommendation to the default plan). No current impact (the real adapter is credential-gated and unexercised in tests). Revisit when the live LLM path is actually turned on: construct the client once (module/instance-level, connection reuse) and set an explicit request timeout (and retry budget) tuned to the interactive `/recommend` latency envelope.

- source_spec: `_bmad-output/implementation-artifacts/6-2-live-llm-gateway-enablement-hardening.md`
  summary: The port's "no raw exception escapes" invariant holds on the non-streaming path only by an unpinned numeric coincidence — `STREAMING_MAX_TOKENS = 16000` sits just below the SDK's real non-streaming `ValueError` threshold (time-based, ~21333 tokens for a 128k model) and the two routed model ids happen not to be in the SDK's per-model cap dict — with no test pinning that relationship, so a future threshold bump or a routing change to a capped model id would let a raw `ValueError` (NOT an `anthropic.APIError`) escape `complete()`.
  evidence: `llm/anthropic_adapter.py` routes to the streaming helper only when `request.max_tokens > STREAMING_MAX_TOKENS` (16000) and wraps the vendor call in `except anthropic.APIError` — which does not cover the SDK's large-`max_tokens` guard, a plain `ValueError` raised by `anthropic/_base_client.py::_calculate_nonstreaming_timeout` when `expected_time > 600s` (≈ `max_tokens > 21333` for 128k context) or when `max_tokens > MODEL_NONSTREAMING_TOKENS[model]` (as low as 8192 for older Opus ids). Verified against the pinned SDK 0.120.2: `claude-opus-4-8`/`claude-sonnet-4-6` are absent from that cap dict and 16000 < 21333, so the `ValueError` is genuinely UNREACHABLE at the current config — which is exactly why the prior review pass deliberately narrowed the `except` from `(anthropic.APIError, ValueError)` to `anthropic.APIError` (over-catching legitimate/programming ValueErrors is worse). No current defect; the coach uses `max_tokens=4096` and the pipeline's broad `except Exception` backstops any leak into the default plan regardless. Revisit if `STREAMING_MAX_TOKENS` is ever raised toward the SDK boundary or `route_model` can return a capped model id: pin the coupling with a test (assert `STREAMING_MAX_TOKENS` stays below the SDK's effective non-streaming ceiling for every routed model), or surgically wrap just the non-streaming `create()` call so a large-`max_tokens` `ValueError` becomes an `LLMTransportError`/`LLMMalformedResponseError` rather than escaping the port.

- source_spec: `_bmad-output/implementation-artifacts/6-3-live-schwab-placement-reconciliation-mapping.md`
  summary: A `decrypt_token` failure inside the new `get_execution_broker` dependency (wrong `TOKEN_ENCRYPTION_KEY` after rotation, or ciphertext tampered/encrypted under another key) raises `TokenEncryptionError` DURING dependency resolution — before the `approve()` handler body — so it surfaces as a raw 500 through the app error envelope rather than the calm reconnect (409) envelope every other broker-session failure uses, breaking the calm/honest/never-red voice for that failure mode.
  evidence: `brokers/factory.py::get_execution_broker` calls `decrypt_token(row.access_token)` / `decrypt_token(row.refresh_token)` with no `try/except`; `brokers/crypto.py::decrypt_token` raises `TokenEncryptionError` on a missing/invalid key or undecryptable ciphertext. Because the exception is raised in a FastAPI `Depends`, `approve()`'s own `try/except` (which maps `SessionIntegrityError`/`OrderScopeError`/`OrderNotPlaceableError` to calm envelopes and releases the claim) never sees it — the claim was not yet taken at that point (dependency resolution precedes the handler), so there is no stranding, only an ugly 500. No current impact (creds are stable; the schwab path is credential-gated and unexercised in the suite). Revisit at go-live: wrap the decrypt in `get_execution_broker` and raise `HTTPException(409, RECONNECT_MESSAGE)` (or a `SchwabNotConfiguredError` the endpoint maps calmly) so an undecryptable token prompts a calm re-link instead of a raw server error.

- source_spec: `_bmad-output/implementation-artifacts/6-3-live-schwab-placement-reconciliation-mapping.md`
  summary: The SchwabAdapter dedupes a re-placement only via its in-request `idempotency_key → order_id` cache (Schwab itself honors NO client key), so if any UNEXPECTED exception (a type not in `place_order`'s catch tuple, or a bug in our own mapping code) escapes AFTER a real placement, `approve()`'s bare `except Exception` releases the claim back to `proposed`, and a retry builds a FRESH adapter with an empty cache that re-sizes and places a SECOND real order.
  evidence: `brokers/factory.py::get_execution_broker` returns a new `SchwabAdapter(token_read_func=...)` per request and `self._orders` is per-instance ("Lives for exactly one request"), so cross-request dedupe is impossible by construction; `api/coach.py::approve` releases the claim on any non-transport `except Exception` post-placement; the adapter's docstring states plainly "Schwab honors no client key". This review broadened/hardened the transport net (added `ArithmeticError`/NaN sanitization so the KNOWN Decimal-comparison leak can no longer escape post-placement), which closes the concrete instances found — but a genuinely novel exception type still routes through the same double-place window. No current impact (schwab path is credential-gated and unexercised; the fake adapter honors its own key). This is the money-bearing tail of the locked "durable cross-request reconciliation is Story 6.7" boundary. Revisit in Story 6.7: give placement a broker-durable idempotency mechanism (e.g. persist the minted key + reconcile an existing order before re-placing) so a released-claim retry can never double-fill real money, and add a cross-request retry test that asserts a second approve after a post-placement escape does NOT place twice.

- source_spec: `_bmad-output/implementation-artifacts/6-3-live-schwab-placement-reconciliation-mapping.md`
  summary: `_account_hash` unconditionally trades `accounts[0]` (the first account Schwab returns) with no selection, display, or confirmation, so a user whose Schwab login exposes more than one account (brokerage + IRA + joint is common) can have a real order land in the WRONG account — e.g. a taxable buy executed inside a tax-advantaged IRA — and the cosigned record does not even capture which account hash was used.
  evidence: `brokers/schwab_adapter/adapter.py::_account_hash` does `hash_value = accounts[0].get("hashValue")` with a comment "v1 uses the first account"; there is no account-selection logic anywhere in the diff, no user-facing account indicator, and `_map_order`/`cosign` never record the account hash. Account selection is outside this story's stated scope (the intent only says "resolve the account hash before placing"), so it was not built — but for a real-money go-live "silently pick the first account" is a genuine user-harm risk, not a cosmetic one. No current impact (schwab path is credential-gated and unexercised). Revisit before multi-account go-live: let the user choose (or explicitly confirm) the trading account, persist the chosen account hash on the decision record, and refuse to place if the linked login exposes multiple accounts without a selection.

- source_spec: `_bmad-output/implementation-artifacts/6-3-live-schwab-placement-reconciliation-mapping.md`
  summary: The new queryable `decision_record.broker_ref` column (like `idempotency_key` before it) is only added via `create_all`, which does NOT `ALTER` an already-created table, and the only `ADD COLUMN IF NOT EXISTS` in the diff lives in a test fixture — so on any pre-existing (deployed) database the first real approve can place a live order and then fail to persist its `broker_ref`, defeating the very column Story 6.7 is meant to consume.
  evidence: `db/models.py` adds `broker_ref` via `mapped_column(String(64), ...)` with an in-code comment admitting "adding this to the model will NOT ALTER an already-created table — a go-live caveat, exactly as `idempotency_key` was"; the only migration in the diff is `tests/test_coach_api.py::ensure_tables` running a raw `ALTER TABLE decision_record ADD COLUMN IF NOT EXISTS broker_ref VARCHAR(64)` — there is no Alembic/app-path migration. `coach/decision_record.py::cosign` sets `record.broker_ref = outcome.broker_ref`, so against a table missing the column the post-placement commit fails after a real order landed. No current impact (fresh dev/test DBs are built by `create_all` with the column present). Revisit before go-live: add a real migration path (Alembic or an idempotent startup `ADD COLUMN IF NOT EXISTS`) that provisions `broker_ref` (and backfills/asserts `idempotency_key`) on existing databases before any live placement.

- source_spec: `_bmad-output/implementation-artifacts/6-3-live-schwab-placement-reconciliation-mapping.md`
  summary: `approve()` runs `cosign(...)` and `session.commit()` AFTER a real placement but OUTSIDE any try/except, and — correctly, for double-order safety — does NOT release the claim on a post-placement failure; so if that cosign/commit raises, the record is stranded in `cosigning` with a real order already placed AND its `broker_ref` never persisted, leaving it unrecoverable even by Story 6.7 (whose reconcile keys on the `broker_ref` the failed commit was supposed to write).
  evidence: `api/coach.py::approve` calls `cosign(record, order_intent, outcome, ...)` then `await session.commit()` after the placement `try` block returns; `coach/decision_record.py::cosign` sets `record.broker_ref = outcome.broker_ref` and `record.status = "cosigned"` in the same uncommitted unit of work, so a commit failure (DB blip, or the missing-column case tracked separately) rolls BOTH back — the record keeps `status="cosigning"` with no `broker_ref`. Because the claim is deliberately NOT released post-placement (the intent contract only mandates release for PRE-placement failures, to avoid double-ordering), the record is neither retryable nor reconcilable: 6.7's durable recovery finds orders by `broker_ref`, which was never written. No current impact (the schwab path is credential-gated and unexercised; cosign's own asserts are near-unreachable in the normal flow since status is freshly `cosigning` and the key matches). This is the money-side of the atomicity gap (place-then-persist is not one atomic unit). Revisit in Story 6.7 alongside durable reconciliation: persist the minted `broker_ref` in the SAME transaction/atomic step as the placement claim (or write a placement-intent row before placing) so a placed order is always recoverable, and give a stranded-in-`cosigning`-with-a-live-order record an explicit recovery path.

### DW-1: Follow-up review still recommended for 6-3-live-schwab-placement-reconciliation-mapping after the damping cap was spent
origin: review-budget-followup
source_spec: `6-3-live-schwab-placement-reconciliation-mapping.md`
severity: low
reason: The follow-up-review damping cap (limits.max_followup_reviews = 1) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260801-161954-6f66; this entry preserves the lingering recommendation for a deliberate later review.
status: open

- source_spec: `_bmad-output/implementation-artifacts/6-4-fixed-point-money-serialization-pass.md`
  summary: A non-finite (`NaN`/`Infinity`) `avg_price` from a malformed Schwab order JSON would serialize verbatim as `"NaN"`/`"Infinity"` on the `/approve` wire AND into the immutable co-sign snapshot — the money serializers render non-finite Decimals literally and the broker adapter's direct-field price extractor does not sanitize them.
  evidence: `brokers/schwab_adapter/adapter.py::_extract_avg_price` returns `Decimal(str(val))` from the `avgFillPrice`/`averagePrice`/`price` direct-field branch with NO `is_finite()` guard — unlike its sibling `_decimal_or_zero` (same file, ~line 521) which explicitly maps non-finite → `Decimal("0")`. A non-finite result flows into `OrderOutcome.avg_price` → `coach/decision_record.py::cosign` persists `format_money(NaN)` (`"NaN"`) in `cosign_snapshot` and `api/coach.py::_to_approve_response` emits it on the wire; verified `format(Decimal('NaN'),'f') == 'NaN'` and `format(Decimal('Infinity'),'f') == 'Infinity'` (non-JSON-number, non-round-tripping, and permanently baked into the immutable record). NOT caused by Story 6.4 — the pre-existing `_money`/`_money_str` helpers already used `format(value,'f')` and produced the same `"NaN"`; 6.4 only unified them and surfaced the gap. Unreachable with current fixtures (crafted order JSON carries finite prices) and never via the Pydantic `WireMoney` portfolio fields (Pydantic 2.13 rejects non-finite Decimals at validation, `allow_inf_nan=False`), so no live impact. Fix belongs with the Schwab adapter / go-live hardening (Story 6.3/6.7 territory): mirror the `_decimal_or_zero` `is_finite()` guard in `_extract_avg_price` (return `None` on non-finite so avg_price stays honestly absent), or reject a non-finite reconciled `avg_price` at the port boundary before it can reach the immutable snapshot.

- source_spec: `_bmad-output/implementation-artifacts/6-5-real-schwab-balances-cash-only-mapping.md`
  summary: `SchwabAdapter.fetch_portfolio()` is a SYNC method doing blocking network I/O (`get_account_numbers` + `get_account`) and is called without `await`/threadpool inside the async request handlers (`refresh_portfolio`, link `callback`), so a real Schwab read blocks the event loop for the duration of the HTTP round-trips, stalling every other concurrent request on that worker.
  evidence: `brokers/port.py::fetch_portfolio` is declared `def` (sync) since Story 2.1/2.3; `brokers/portfolio.py::reconcile_portfolio` calls `snap = broker.fetch_portfolio()` with no offload; Story 6.5 wired the real blocking `client.get_account(...)` into that sync method. Before 6.5 the Schwab body raised immediately (stub) so nothing blocked; now the credential-gated real path does. No current impact (schwab path is credential-gated/unexercised in tests; single-user dev). Revisit at go-live: run the blocking fetch via `anyio.to_thread.run_sync(...)` (or adopt schwab-py's async client), which likely means making the port's `fetch_portfolio` contract async (touches the fake adapter + reconciler too).

- source_spec: `_bmad-output/implementation-artifacts/6-5-real-schwab-balances-cash-only-mapping.md`
  summary: On the REAL Schwab path `fetch_portfolio` stamps `as_of = datetime.now(UTC)` at fetch time (Schwab `currentBalances` carries no reliable snapshot timestamp), so the reconcile-wins staleness protection — the epic invariant that "a stale/older snapshot never clobbers newer cached truth", now keyed on `portfolio_balance.as_of` — is effectively inert for real reads: every `/refresh` produces a strictly-newer `as_of`, so an out-of-order/overlapping refresh always wins.
  evidence: `brokers/schwab_adapter/adapter.py::fetch_portfolio` returns `PortfolioSnapshot(as_of=datetime.now(timezone.utc), ...)`; `brokers/portfolio.py::reconcile_portfolio` compares `incoming_as_of <= cached_as_of` to skip stale writes. With wall-clock-at-parse `as_of`, two concurrent `POST /api/portfolio/refresh` (or a slow request landing after a fast one) both look "newer" and the later-committing one wins regardless of which read fresher broker state. The spec's I/O matrix documents `as_of = now(UTC)` as expected, so this is as-designed for v1 — but it silently guts the anti-clobber guarantee for the real path (the fake path is deterministic and staleness works). Revisit if Schwab exposes a broker-side balance/snapshot timestamp, or gate refresh concurrency; ties to the two-table atomicity item below.

- source_spec: `_bmad-output/implementation-artifacts/6-5-real-schwab-balances-cash-only-mapping.md`
  summary: `reconcile_portfolio` now writes TWO tables (`portfolio_balance` upsert + `portfolio_cache` delete-then-add) in "one commit" but as a read-modify-write with NO row lock / `SELECT ... FOR UPDATE` / serializable isolation, so two overlapping reconciles (two `/refresh`, or `/refresh` racing import-on-connect) can both pass the staleness check and interleave — duplicating/dropping holdings and (absent the new unique index catching it) risking a duplicate balance row.
  evidence: `brokers/portfolio.py::reconcile_portfolio` reads existing cache + balance, decides stale-or-not, then deletes/adds/commits — classic TOCTOU; the "single writer" is a code convention, not a DB guarantee. Story 6.5 spreads the atomic unit across two tables. The `UniqueConstraint("owner_id")` added on `portfolio_balance` this pass converts a concurrent double-insert into a loud IntegrityError (partial mitigation), but the holdings interleave and the read-modify-write window remain. No current impact (single-user dev; schwab path unexercised). This is the same class as the recorded Epic 4 atomicity gap (sole-writer/canary convention cannot express atomic claims). Revisit with the broader atomicity hardening: claim/lock the user's projection (row lock or an advisory lock keyed on owner_id) for the read-modify-write, or push reconcile-wins into a conditional `UPDATE ... WHERE as_of < :incoming`.

- source_spec: `_bmad-output/implementation-artifacts/6-5-real-schwab-balances-cash-only-mapping.md`
  summary: Re-linking a brokerage account deletes the user's `BrokerageToken` rows but nothing clears the `portfolio_balance` (or `portfolio_cache`) projection, so a re-link to a DIFFERENT account whose incoming snapshot is judged stale (`as_of <= cached`) leaves the prior account's cash/holdings on the dashboard until a strictly-newer snapshot reconciles.
  evidence: `api/brokerage.py::callback` deletes prior token rows then reconciles, but the balance row is upsert-only and never deleted on unlink/re-link; reconcile-wins keyed on `portfolio_balance.as_of` will SKIP an incoming snapshot with an equal/older `as_of`. Practical exposure is low on the real path (Schwab `as_of = now(UTC)` always wins — see the staleness item above) and it mirrors the pre-existing behavior that `portfolio_cache` rows also survive re-link until reconciled, so it is a projection-hygiene gap rather than a fresh regression. No current impact (single-user dev). Revisit alongside unlink/re-link handling: clear the user's portfolio projection (both tables) in the same transaction that replaces token rows, so a new account can never inherit the old account's cash under staleness skip.

- source_spec: `_bmad-output/implementation-artifacts/6-5-real-schwab-balances-cash-only-mapping.md`
  summary: `get_reading_broker` shares the `_bind_user_token` decrypt helper, so a `decrypt_token` failure (rotated `TOKEN_ENCRYPTION_KEY` / corrupt ciphertext) during dependency resolution on `POST /api/portfolio/refresh` escapes as a raw 500 rather than a calm reconnect envelope — the SAME root cause already logged for 6.3's `get_execution_broker`, now reachable on a user-facing read endpoint.
  evidence: `brokers/factory.py::_bind_user_token` calls `decrypt_token(row.access_token)`/`decrypt_token(row.refresh_token)` with no `try/except`; `brokers/crypto.py::decrypt_token` raises `TokenEncryptionError` on a missing/invalid key or undecryptable ciphertext. Because it is raised inside a FastAPI `Depends` for `refresh_portfolio` (which — unlike the link callback — has no swallowing try/except), it surfaces as a raw 500, not the calm reconnect (409) envelope. This is the read-path twin of the already-logged 6.3 `get_execution_broker` decrypt gap; no current impact (creds stable; schwab path credential-gated). Fix both at go-live: wrap the decrypt in `_bind_user_token` and raise a `SchwabNotConfiguredError`/`HTTPException(409, RECONNECT_MESSAGE)` so an undecryptable token prompts a calm re-link instead of a server error.

- source_spec: `_bmad-output/implementation-artifacts/6-5-real-schwab-balances-cash-only-mapping.md`
  summary: The now-authoritative-elsewhere `portfolio_cache.cash` column is vestigial-but-still-written — the single writer stamps `cash=snap.cash` on every holdings row even though idle cash is now sourced exclusively from `portfolio_balance`, leaving a permanently-written duplicate field no reader consults (a latent drift/confusion surface). The spec's own Design Notes explicitly flagged dropping it as "a deferred cleanup (log to deferred-work)"; the implementing/first-review pass never logged it, so this entry captures it.
  evidence: `brokers/portfolio.py::reconcile_portfolio` still calls `cache_repo.add(..., cash=snap.cash, ...)` per holding, and `db/models.py::PortfolioCache` retains the `cash Numeric(20,2)` column with a docstring now describing it as a "retained DENORMALIZED SNAPSHOT COPY … no reader derives idle cash from a holdings row anymore." It was kept written deliberately to stay schema-additive (avoid a nullable `ALTER`, since `create_all` never ALTERs), and both tables are written from the same `snap.cash` in one commit so they cannot drift at rest today — hence low consequence. But a dead-but-written money column is exactly where future drift/misread bugs hide (a stray query reading `portfolio_cache.cash`, or a partial-write path that ever diverges the two). Revisit as a focused schema-cleanup once a migration path exists: stop writing the column and drop it (or make it nullable and cease writes), so `portfolio_balance` is the single physical home of idle cash.

- source_spec: `_bmad-output/implementation-artifacts/6-6-decisions-history-scale-hardening.md`
  summary: The new `(owner_id, co_signed_at)` composite index backing the paginated Decisions history read is added only via `create_all`, which does NOT `ALTER` an already-created table, and the only migration is a `CREATE INDEX IF NOT EXISTS` in the test fixture — so on any pre-existing (deployed) database the paginated `GET /api/coach/decisions` runs as a full scan + filesort, unmet the story's whole "scale-safe" promise, exactly like the `idempotency_key` (6.1) and `broker_ref` (6.3) columns before it.
  evidence: `db/models.py::DecisionRecord.__table_args__` adds `Index("ix_decision_record_owner_co_signed_at", "owner_id", "co_signed_at")` with an in-code comment admitting it "will NOT ALTER an already-created table"; the only index-creation outside the model is `tests/test_coach_api.py::ensure_tables` running a raw `CREATE INDEX IF NOT EXISTS`. No Alembic/app-path migration exists (the project uses `Base.metadata.create_all`). No current impact (fresh dev/test DBs build the index via `create_all`). Revisit at go-live alongside the already-logged `broker_ref`/`idempotency_key` index steps: add a real migration path (Alembic or an idempotent startup `CREATE INDEX IF NOT EXISTS`) that provisions this index on existing databases before Decisions history sees volume.

- source_spec: `_bmad-output/implementation-artifacts/6-6-decisions-history-scale-hardening.md`
  summary: A decision_record stuck in the transient `cosigning` state (a crash between `claim_for_cosign` and either `cosign` or `release_claim`) has no reclamation path — the new retention prune deletes `proposed` rows only (as the Story 6.6 AC scopes it), so an orphaned `cosigning` row is never surfaced in history AND never pruned, accumulating as a permanent zombie.
  evidence: `coach/decision_record.py::prune_stale_proposed_decisions` hard-pins `status == "proposed"`; `list_cosigned_decisions` filters `status == "cosigned"`, so a `cosigning` row is invisible to both the user and the pruner. The normal 6.1 lifecycle releases the claim on refusal/failure (`release_claim`), so this only occurs on a hard crash / lost process mid-approve — a pre-existing gap in the Story 6.1 claim lifecycle, not introduced here, and with no current impact (single-user dev, schwab path credential-gated). Revisit when hardening the live approve path: add a bounded reclamation (e.g. release `cosigning` rows older than a short window back to `proposed`, or a supervised reconcile) so a crash mid-claim cannot strand a decision.

- source_spec: `_bmad-output/implementation-artifacts/6-7-durable-cross-request-timeout-reconciliation.md`
  summary: The new `POST /decisions/{id}/reconcile` endpoint has no atomic claim / row lock, so two concurrent reconciles of the same decision both read the broker (two round-trips where the design says one) and last-writer-wins the `reconciliation_snapshot` — a transiently-worse read (e.g. a hiccup `TIMEOUT`) landing after a `FILLED` read can regress the persisted money truth.
  evidence: `api/coach.py::reconcile_decision` calls `load_decision` → `reconcile_pending_decision` → `record_reconciliation` + `commit` with no `SELECT ... FOR UPDATE` / conditional `UPDATE`, unlike `approve` which was hardened with `claim_for_cosign` (an atomic `proposed→cosigning` claim) for exactly this same-decision concurrency hazard. The reconcile path is READ-ONLY (no double-place / no phantom fill), and this pass added an in-writer terminal-monotonicity guard (`record_reconciliation` no-ops on an already-terminal effective status) which blocks the worst regression (filled→pending) within a single loaded record but NOT across two sessions that both loaded a non-terminal record before either committed. No current impact (single-user dev; schwab path credential-gated/unexercised). Same atomicity class as the recorded Epic 4 "sole-writer/canary convention cannot express atomic claims" gap. Revisit with the broader atomicity hardening: guard the reconcile read-modify-write with a row/advisory lock keyed on the decision id, or push reconcile-wins into a conditional `UPDATE ... WHERE` on the outcome status, and add a concurrent-reconcile test asserting the snapshot never regresses.

- source_spec: `_bmad-output/implementation-artifacts/6-7-durable-cross-request-timeout-reconciliation.md`
  summary: `SchwabAdapter.get_order_status_by_ref`'s `except Exception` fence maps deterministic config/auth faults (`SchwabNotConfiguredError` from a missing token or a malformed account-numbers body, raised by `_trading_client`/`_account_hash`) into `OrderStatus.TIMEOUT`, so a reconcile whose real problem is a bad session/token is surfaced as `timeout` + `needs_reconfirmation=true` and every retry re-launders the same fault — a soft dead-end with no honest "your connection needs a reconnect" signal, unlike the epic's `session/config → 409 reconnect` vs `transport → timeout` taxonomy.
  evidence: `brokers/schwab_adapter/adapter.py::get_order_status_by_ref` wraps `self._trading_client()` and `self._account_hash(client)` inside the same fence as the `client.get_order(...)` transport read; both helpers raise `SchwabNotConfiguredError` (a deterministic config fault, not transport indeterminacy) and the bare `except Exception` returns `TIMEOUT` with the ref preserved. The fence correctly prevents a raw leak and a phantom fill (good), but it over-captures a distinguishable, non-transient class. The entry gate `require_live_broker_session` reduces this (a flagged-dead session 409s before the handler) but does not eliminate it (a present-but-unusable token, or a malformed account body, is not flagged non-live). No current impact (schwab path credential-gated/unexercised). Revisit at go-live: surface pre-read config/auth faults distinctly (a calm 409 reconnect) BEFORE the transport fence, mirroring how `place_order` treats pre-placement `SchwabNotConfiguredError` separately from the transport→TIMEOUT net.

- source_spec: `_bmad-output/implementation-artifacts/6-7-durable-cross-request-timeout-reconciliation.md`
  summary: `partial` is classified terminal (`coach/decision_record.py::_TERMINAL_STATUSES = {"filled","partial","rejected"}`), so once a reconcile records a `partial` fill the engine short-circuits every future reconcile — a market order that was partially filled and then COMPLETED to `filled` can never be durably re-reconciled, and it reports `needs_reconfirmation=false` (positively confirmed) on a state that is not the final state, mildly undercutting the epic's "always see the true reconciled state".
  evidence: `coach/execution.py::reconcile_pending_decision` returns early with `reconciled=False` when `_is_terminal(effective_outcome_status(record))`, and `_is_terminal("partial")` is True; the story's own I/O matrix "Already terminal" row only exemplifies `filled`/`rejected`, not `partial`. Whether a partial fill on a whole-share MARKET order is a stable end-state is genuinely a product decision, not a clear bug — treating it as settled is defensible for v1, so this was left as-is rather than flipped unilaterally. No current impact (schwab path credential-gated/unexercised; partial-then-complete is uncommon for a market order). Revisit as a product decision: if a partial should remain reconcilable until `filled`/`rejected`, drop `partial` from `_TERMINAL_STATUSES` (it stays confirmed → `needs_reconfirmation=false`, just re-readable) and add a partial→filled re-reconcile test; otherwise document partial-as-terminal explicitly.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-production-db-migration-path.md`
  summary: The Story 7.1 startup migration provisions the two unique backstops (`uq_decision_record_idempotency_key`, `uq_portfolio_balance_owner`) as plain unique INDEXES via `CREATE UNIQUE INDEX IF NOT EXISTS`, whereas `create_all` on a fresh DB emits them as unique CONSTRAINTS (backed by same-named indexes) — so a fresh prod DB and a carried-over/migrated prod DB end up with structurally different catalog objects under identical names (a `pg_constraint` row on one, not the other), a permanent, un-reconciled schema drift.
  evidence: `db/models.py` declares both as `UniqueConstraint(...)`; the migration in `db/migrations.py::INDEX_STATEMENTS` creates them with `CREATE UNIQUE INDEX IF NOT EXISTS`. This is functionally benign for the go-live guarantee — a unique index enforces the double-place backstop with the identical `IntegrityError`, and there is no `ON CONFLICT ON CONSTRAINT <name>` / named-constraint upsert anywhere that would hard-require the constraint form (verified). The index form was a deliberate choice: it is cleanly idempotent (Postgres has no `ADD CONSTRAINT IF NOT EXISTS`, which would need a `DO`/`pg_constraint` block) and it matches the proven `CREATE UNIQUE INDEX IF NOT EXISTS` pattern already used in the test fixtures. No current impact. Revisit if/when Alembic (or any versioned baseline) is adopted: reconcile the two backstops to the constraint form (or codify the index form in the ORM) so a `pg_dump`/Alembic-autogenerate diff between a fresh and a migrated DB is clean.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-production-db-migration-path.md`
  summary: The startup migration's `CREATE [UNIQUE] INDEX` statements run inside a single transaction (so `CONCURRENTLY` is impossible) and take a normal build-time lock on `decision_record`; on a large carried-over table this would stall the app's own request path during the startup that gates readiness.
  evidence: `db/migrations.py::run_startup_migrations` wraps all DDL in one `engine.begin()`; non-`CONCURRENTLY` `CREATE UNIQUE INDEX` locks the table against writes for the build duration. Acknowledged as a deliberate v1 tradeoff in the spec's Design Notes ("normal build, brief table lock … CONCURRENTLY unnecessary for v1's data volume; revisit if a table grows large") — negligible at the current beginner-investor scale. Revisit before the tables grow large: move index creation to an out-of-transaction `CREATE INDEX CONCURRENTLY` path (with INVALID-index cleanup on failure), likely as part of adopting a real migration framework.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-production-db-migration-path.md`
  summary: The startup migration re-executes its full statement list (including the `WHERE idempotency_key IS NULL AND status='proposed'` backfill, a full-table predicate with no supporting index at backfill time) on every single app boot forever, with no `schema_migrations`/version table to short-circuit an already-applied migration — idempotent, but unbounded startup DDL churn and a per-boot seq-scan that grows with the table.
  evidence: `db/migrations.py::run_startup_migrations` has no completion marker; the FastAPI lifespan calls it on every startup. Cheap now (small tables), a latent startup-latency cost as `decision_record` grows. Deliberately not addressed here — a migration-version ledger is essentially reinventing Alembic's `alembic_version`, which the story explicitly scoped out. Revisit as part of adopting a versioned migration framework: gate applied migrations behind a version table so each boot skips already-run steps.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-generalized-atomic-claim-recoverable-placement.md`
  summary: The `reclaim_orphaned_cosigning` reclaimer (and the pre-existing `prune_stale_proposed_decisions`) are correct invocable functions but are wired to NO scheduler/cron/startup hook, so orphaned `cosigning` rows carrying live orders are never actually recovered in a running deployment; the reclaimer also loads all matching candidates across all owners into memory with no LIMIT/paging, and there is no operator surface exposing how long a row has been `cosigning`.
  evidence: A tree grep finds zero callers of `reclaim_orphaned_cosigning` outside its own tests; the spec deliberately scoped scheduler wiring out ("wiring the reclaimer to a periodic runner is out of scope — it is an invocable function like `prune_stale_proposed_decisions`"), consistent with the digest job and the 6.6 pruner which are likewise externally invoked. No current impact (single-user dev, schwab path credential-gated, and the new durable-`broker_ref`-first ordering makes an orphan rare). Revisit at go-live: introduce a supervised runner (cron/startup task) that invokes the reclaimer (and the pruner) on a bounded page size, plus a log/metric for rows lingering in `cosigning`, so a crash-orphaned live order is actually recovered rather than only recoverable in principle.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-generalized-atomic-claim-recoverable-placement.md`
  summary: `decision_record.broker_ref` is `VARCHAR(64)` and neither `persist_broker_ref` (Story 7.2) nor `cosign` (Story 6.3) validates/truncates the broker reference length, so a real Schwab order id longer than 64 chars would raise a `DataError` at the post-placement `persist_broker_ref` commit — after a live order exists — converting a successful placement into an uncaught 500 with the row left `cosigning` and NO durable ref (the exact zombie the story eliminates).
  evidence: `api/coach.py::approve` calls `persist_broker_ref` (and later `cosign`) after `execute_approved_order` returns; `db/models.py` defines `broker_ref` as `String(length=64)`; the `IntegrityError`/`DataError` from an over-length value is not the caught pre-placement refusal set, and no length guard exists. This is a pre-existing constraint (broker_ref has been `String(64)` since 6.3) surfaced by the new earlier write site, and ties to the epic's success criterion of confirming the guessed live field mappings against real Schwab payloads. No current impact (schwab path credential-gated/unexercised; the fake adapter's refs fit). Revisit in Story 7.6 (real-payload confirmation): confirm the real Schwab order-id length envelope and either widen the column or validate/normalize `broker_ref` before the post-placement persist so an over-length ref cannot strand a live order.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-generalized-atomic-claim-recoverable-placement.md`
  summary: The reconcile endpoint now persists the reconciled outcome UNDER A ROW LOCK so the in-writer monotonic terminal guard cannot regress persisted money truth, but it still builds its HTTP response from the freshly-read `result.outcome`, so under two concurrent reconciles of one decision a transiently-worse read (e.g. `timeout`) whose persist was correctly no-op'd by the guard still returns `status=timeout` + `needs_reconfirmation=true` to that caller for an already-`filled` (terminal) decision — the persisted truth is protected but the user-facing surface is walked back.
  evidence: `api/coach.py::reconcile_decision` calls `lock_decision(...)` then `record_reconciliation(locked, outcome=result.outcome)` (which no-ops when `effective_outcome_status(locked)` is already terminal — persisted truth safe), but then unconditionally does `outcome = result.outcome` and returns it, so the losing reconcile's response reflects its own broker read rather than the persisted post-guard state. No DB regression / no double-place, and it self-heals on the next reconcile (which reads the terminal state), so consequence is a transient wrong signal — but a user can be told a filled order "needs reconfirmation." The Story 7.2 test `test_concurrent_reconcile_never_regresses_terminal_outcome` asserts only the persisted `effective_outcome_status`, never the response body, so this is uncovered. Revisit by deriving the response `status`/`filled_qty`/`avg_price`/`needs_reconfirmation` from the locked+persisted record after the guard runs (return the standing terminal truth when the guard no-ops), plus a concurrent-reconcile test asserting the RESPONSE never regresses.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-generalized-atomic-claim-recoverable-placement.md`
  summary: `reclaim_orphaned_cosigning` iterates every candidate orphan issuing one rowcount-gated conditional UPDATE each and commits ONCE at the end, with no per-row error isolation, so a single failing per-row UPDATE aborts the whole transaction and discards every reclamation already done earlier in that batch — one poison row can block all reclamation until it is manually resolved.
  evidence: `coach/decision_record.py::reclaim_orphaned_cosigning` loops `conditional_claim(session, update(DecisionRecord)...)` per candidate with no per-row `try/except` and a single trailing `await session.commit()`; any row-level error leaves the session in a failed transaction and rolls back all prior winners in that run. An idempotent re-run retries the still-`cosigning` good rows, but a persistently-bad row starves forward progress indefinitely. Trigger probability is low (the UPDATE only sets `status`/`co_signed_at`/a JSON-serializable recovery snapshot) and the reclaimer is not yet scheduler-wired, so no current impact. Revisit alongside the already-logged reclaimer runner/paging hardening: isolate each row (per-row try/except + continue, or per-row commit) and log/skip a poison orphan so one bad row cannot block the batch.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-calm-envelopes-provider-match-live-broker.md`
  summary: Story 7.3 gave the READ-ONLY reconcile read a calm-409 mapping for a deterministic `SchwabNotConfiguredError`, but the SAME config-class fault still surfaces as a raw 500 at the other live broker seams the story's AC did not enumerate — `/approve` placement (`place_order`'s `_account_hash` empty-account / missing-hash / non-dict body raises `SchwabNotConfiguredError`, caught by the approve handler's bare `except Exception` which releases the claim and re-raises → app-level 500) and `POST /portfolio/refresh` read (`fetch_portfolio` raises `SchwabNotConfiguredError`/`SchwabReadError` with no handler try/except → raw 500) — plus the unlinked-user missing-token `SchwabNotConfiguredError` raised inside `_bind_user_token` (factory.py:110) during FastAPI dependency resolution, which bypasses every handler try/except.
  evidence: `api/coach.py::approve` maps only `SessionIntegrityError`/`OrderScopeError`/`OrderNotPlaceableError` to calm envelopes; a pre-placement `SchwabNotConfiguredError` from `SchwabAdapter._account_hash` (adapter.py:600-616 — empty account list, non-dict first element, or missing `hashValue`, all deliberately config-class per that method's docstring) falls to the `except Exception` at coach.py:548 → `release_claim` + `raise` → the app's `@app.exception_handler(Exception)` returns 500 "An unexpected error occurred." (breaks NFR8 calm/honest/never-red on the real-money placement seam). `api/portfolio.py::refresh_portfolio` has no try/except around the read at all, so a read-time `SchwabNotConfiguredError`/`SchwabReadError` is a raw 500 too. And `_bind_user_token` raises `SchwabNotConfiguredError(missing_message)` (factory.py:110) for a tokenless user during dependency resolution — mostly shadowed by `require_live_broker_session` (an unlinked user has no live session) but TOCTOU-reachable if the token row is deleted between the two independent dependency queries. All three are PRE-EXISTING (untouched by the 7.3 diff, which only wrapped the decrypt path and the reconcile read) and outside the 7.3 AC's enumerated triggers (which scope `/approve`+`/refresh` to the *decrypt* fault and the config fault to the *reconcile read*), so they were not in scope for 7.3. No current impact (Schwab path is credential-gated/unexercised). Revisit as a focused calm-mapping pass (natural companion to Story 7.6 real-payload confirmation): add `except SchwabNotConfiguredError` → 409 `RECONNECT_MESSAGE` at `/approve` (release the claim first — it is pre-placement, so retryable) and at `/refresh`, and either map the tokenless `_bind_user_token` case to the same calm 409 or resolve the session+token in one query so the TOCTOU cannot open; add symmetric tests asserting 409 + `RECONNECT_MESSAGE` + (for approve) claim released back to `proposed` and broker `calls == []`.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-llm-live-path-latency-robustness.md`
  summary: Story 7.4 made the anthropic gateway a process-shared singleton (factory memo) whose SDK client is lazily built and cached on the instance, and the factory memo is a module-level dict — neither the lazy `self._client` build nor the cache read-modify-write is guarded by a lock, so a threadpool-concurrent caller could build duplicate clients (leaking an httpx pool) or race the single-slot swap.
  evidence: `llm/anthropic_adapter.py::complete` does check-then-build `if self._client is None: self._client = anthropic.Anthropic(...)`; `llm/factory.py::get_llm_gateway` does `get()`-then-`clear()`-then-`__setitem__` on `_ANTHROPIC_GATEWAY_CACHE` with no lock. BENIGN today: `get_llm_gateway`/`complete()` are sync with no `await` points and are called on the single-threaded event loop (coach/pipeline.py), so there is no true concurrency; worst case under a race is a discarded duplicate client (last-writer-wins), never incorrect data. Becomes a REAL race if Story 7.5 (or any later change) offloads `complete()` or `get_llm_gateway()` onto a threadpool (`anyio.to_thread`) to unblock the event loop. Revisit when the LLM call is moved off the event loop: guard the lazy client build with a per-instance `threading.Lock` (double-checked) and the factory cache mutation with a module-level lock, so connection reuse holds under concurrency instead of thrashing.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-live-read-robustness-multi-account-safety.md`
  summary: The re-link projection clear (brokerage callback) fires on EVERY callback, including a routine same-account token re-auth — so a transient import-on-connect failure right after re-auth leaves the user's portfolio temporarily empty until the next /refresh; gate the clear on a DETECTED account change (e.g. persist the source account hash on portfolio_balance and compare, or read get_account_numbers before clearing) so same-account re-auth preserves the projection.
  evidence: `ballast/backend/api/brokerage.py:247-253` unconditionally deletes portfolio_cache + portfolio_balance; the callback is the re-auth path (Story 2.2), and import-on-connect is best-effort-swallowed (`:263-271`). Story 7.5 deliberately chose empty-over-wrong (spec I/O matrix "Re-link import fails" row), but the clear cannot currently distinguish same-account re-auth from a different-account re-link. Self-heals on the next successful /refresh; low-impact for the single-account go-live case.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-live-read-robustness-multi-account-safety.md`
  summary: The blocking-read offload (`anyio.to_thread.run_sync(broker.fetch_portfolio)`) shares FastAPI/Starlette's default ~40-token worker threadpool with every other sync dependency/endpoint, so many concurrent multi-second Schwab reads could starve that shared pool; isolate broker reads behind a dedicated anyio `CapacityLimiter` if concurrency grows.
  evidence: `ballast/backend/brokers/portfolio.py:157` + the spec Design Note "composes with FastAPI's own threadpool limiter" (i.e. shares the same budget). Net improvement over blocking the event loop (the original bug); not a near-term risk at single-investor go-live scale, but a real scaling ceiling.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-6-live-preflight-payload-shape-harness.md`
  summary: The pre-flight `to_shape` reducer shapes only element [0] of an array, so payload-shape drift in later `positions[]` elements (or a non-text-first Anthropic content block) is invisible — a report caveat discloses it, but full coverage needs a per-element shape-merge (union of keys / worst-case type per key).
  evidence: `preflight/capture.py` `to_shape` builds `{"type":"array","len":N,"item": to_shape(payload[0])}`; the production `fetch_portfolio` mapper iterates ALL positions and `_parse_message` searches all content blocks for the text one, so drift on a non-first element would parse-fail live yet report OK here.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-6-live-preflight-payload-shape-harness.md`
  summary: The read-only harness's token seam confirms only the Ballast-reconstructed token dict, not Schwab's raw OAuth token-endpoint payload; a true read-only token drift check needs a capture tap at the OAuth-exchange boundary (or wiring the harness to drift-check the link-time capture) rather than the reconstructed steady-state dict.
  evidence: `run._drive_default` drives `_to_broker_tokens(token_read_func())` where `factory._token_dict_from_broker_tokens` reconstructs exactly `{access_token, refresh_token, token_type, expires_at}` (the mapper's own fields), so the token seam always PASSes regardless of the live Schwab shape; `factory.py:58-60` already notes "the exact live round-trip is a documented go-live manual check."

- source_spec: `_bmad-output/implementation-artifacts/spec-7-6-live-preflight-payload-shape-harness.md`
  summary: The pre-flight CLI entrypoint (`preflight.run.main`) builds its broker via `get_broker()`, which returns a token-UNBOUND `SchwabAdapter()`; token binding only happens in the async FastAPI deps (`get_execution_broker`/`get_reading_broker`) that need a request DB scope+session — so a real `python -m preflight.run` produces an all-INCOMPLETE report for the four Schwab seams (token/account/portfolio/quote) and confirms only the Anthropic seam. The whole point of the harness — a live drift check before Story 7.7's real order — cannot be met through the shipped CLI without additional token-binding wiring.
  evidence: `brokers/factory.py::get_broker` (factory.py:44-48) returns `SchwabAdapter()` with no `token_read_func`; `preflight/run.py::main` (run.py:295-298) calls `broker = get_broker()`. In `_drive_default` (run.py:776-791), `getattr(broker, "_token_read_func", None)` is `None` → token step records "broker has no _token_read_func bound", and `broker._trading_client()` raises `SchwabNotConfiguredError` (adapter guards `_token_read_func is None`) → account_numbers/quote skipped and `fetch_portfolio` fails → all four Schwab seams INCOMPLETE. Fails LOUD and SAFE (INCOMPLETE + explicit drive-error lines, CLI exit 1 — never a false PASS), and every offline test injects a token-bound `SchwabAdapter(token_read_func=...)` directly so the suite is green while `main()` is unexercised. The root cause is the pre-existing architectural absence of a standalone (non-request) per-user token-binding path (tokens are encrypted per-user in the DB, bound only during FastAPI dependency resolution); the thin CLI wrapper merely surfaced it, and correctly wiring it needs a decision on how the standalone run authenticates as a specific user (which user / interactive prompt / env-specified id / DB session lifecycle). Revisit before/at the Story 7.7 real-money gate (MasterB runs the harness himself when told ready): give `main()` a token-bound reading broker — e.g. an operator script that loads+decrypts the linked user's token and calls `run(broker=bound, gateway=...)`, or a `--user`/scope-driven binding — and add a test exercising the real `main()`/`get_broker()` wiring end to end.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-6-live-preflight-payload-shape-harness.md`
  summary: The portfolio seam reports a false DRIFT for an all-cash account (empty `positions[]`): the three declared position subfields (`instrument.symbol`, `longQuantity`, `marketValue`) read MISSING when `positions` is an empty array, so a first-class-supported all-cash linked account (the Story 6.5 cash-only case) trips the go-live gate even though nothing is actually wrong.
  evidence: `preflight/drift.py` declares `securitiesAccount.positions.[]`-nested subfields (drift.py:444-452); for an empty list `to_shape` emits `{"type":"array","len":0,"item":None}` (capture.py:225-229) and `_descend` short-circuits the `[]` step when `item is None` → `(None, key, None)` (drift.py:527-529) → each subfield MISSING → portfolio seam DRIFT. An all-cash account (cash > 0, zero holdings) is explicitly supported (portfolio_balance sources cash independent of holdings, Story 6.5) and is a valid live shape, yet the harness reports DRIFT on it. Safe-side (a false DRIFT, never a false PASS — it over-warns, it does not falsely confirm) and untested (`_FakeClient` always ships one position). Revisit with a proper empty-array drift semantics: when a declared array parent is present but empty, treat its element subfields as not-applicable / vacuously-satisfied (or a distinct EMPTY state) rather than MISSING, and add an empty-positions all-cash portfolio test asserting PASS (or a non-DRIFT verdict).

- source_spec: `_bmad-output/implementation-artifacts/3-6-hypothetical-drawdown-precedent.md`
  summary: The live (non-hypothetical) aggregate precedent statement does not pluralize its match count ("In 1 similar drops, it recovered…") — a pre-existing grammar blemish in `_build_event_precedent_record`'s live branch, surfaced while fixing the same issue on the new hypothetical branch.
  evidence: `precedent/engine.py` live-path statement (the `else` branch of the hypothetical/live split) reads `f"In {instance_count} similar drops, …"` with no singular form; the hypothetical branch was given a `drop_word` fix in Story 3.6 but the live branch (unchanged by this story, so out of scope) still hard-codes "drops". `instance_count == 1` is reachable for a narrow live band; low severity, cosmetic.

- source_spec: `_bmad-output/implementation-artifacts/3-6-hypothetical-drawdown-precedent.md`
  summary: `HeadlineContextualizer`'s scenario chips can render a stale record on rapid switching — the fetch guard (`mounted.current`) protects against unmount but NOT against out-of-order responses, so an earlier request resolving after a later one overwrites the newer result (and the framing/aria-pressed then reflect the wrong scenario).
  evidence: `HeadlineContextualizer.jsx::fetchPrecedent` awaits `apiFetch` then `setRecord(data)` guarded only by `if (!mounted.current) return`; two in-flight chip clicks have no request-token/sequence check, so the last-to-resolve wins regardless of click order. Shared limitation with `RecoveryPrecedent`'s `active` pattern; low probability (chips disable while `inFlight`, narrowing but not closing the window). Fix with a per-request sequence token (`const my = ++reqId.current; … if (my !== reqId.current) return`).

- source_spec: `_bmad-output/implementation-artifacts/3-6-hypothetical-drawdown-precedent.md`
  summary: Both the live and hypothetical aggregate precedent statements assert the recovery median over a denominator that includes never-recovered episodes ("in N comparable drops … it recovered to breakeven in a median of X days"), where `instance_count` (N) counts all band matches but `recovery_days_median` is computed over recovered episodes only — so a reader infers "all N recovered in ~X days" when only a subset did.
  evidence: `precedent/engine.py::_build_event_precedent_record` builds `recovery_days_median` from `recovery_days_list` (recovered episodes only, ~line 364) yet phrases the sentence with `instance_count` as the subject ("In {instance_count} … it recovered … in a median of {recovery_days_median} …"). The pattern pre-exists in the untouched live branch (out of Story 3.6 scope) and is mirrored into the new hypothetical branch. Direction is conservative-ish (it slightly overstates the recovery base rate, never fabricates a worse-than-real one), so low/medium honesty consequence. Fix by qualifying the denominator (e.g. "in R of N comparable drops that recovered, the median was X days") or citing the recovered count explicitly, in both branches.

## Deferred from: code review of 8-1-marketable-limit-orders-order-model (2026-08-04)

- source_spec: `_bmad-output/implementation-artifacts/8-1-marketable-limit-orders-order-model.md`
  summary: No tick-size / penny quantization of `limit_price` before it is sent to Schwab — a sub-penny or over-precise limit (e.g. `"100.999"`) passes every gate and would be REJECTED by a live broker at go-live.
  evidence: `execution.validate_order_intent` and `api/coach.py::OrderIntentIn._validate_order_matrix` only check `limit_price.is_finite() and > 0`; `schwab_adapter/adapter.py:430` then does `format_money(limit_price)` (money.py `format(value, "f")`), preserving the submitted scale verbatim into `equity_buy_limit(symbol, quantity, price_str)`. Equities ≥ $1 must trade in $0.01 increments; a non-$0.01-aligned price becomes a broker REJECTED. Not reachable in Story A (no order-entry UI — Story C; the LLM coach never sets a price). Belongs to the Story C UI-boundary validation and/or the Story 7.7 real-money gate — decide reject-vs-round-to-tick there. Reason for deferral: no live/UI path exists in Story A to trigger it; the AC-3 field matrix requires nothing beyond finite/>0.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-resting-order-lifecycle.md`
  summary: Cancel evaluates order terminality on an UNLOCKED record (load-then-lock), so two concurrent `/cancel` requests could both reach `broker.cancel_order`; harden the shared load-then-lock concurrency pattern (also used by `/reconcile`).
  evidence: `api/coach.py::cancel_decision` calls `load_decision` (no FOR UPDATE), then `cancel_pending_decision` decides to touch the broker off that unlocked snapshot, and only re-`lock_decision`s to persist. Broker cancel is idempotent so harm is negligible today, but the terminal check is not under the row lock. Mirrors the pre-existing `/reconcile` shape.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-resting-order-lifecycle.md`
  summary: If `lock_decision` returns None after a real broker cancel, the persist is silently skipped while a 200 is still returned — the broker order is cancelled but the local record stays `pending`.
  evidence: `api/coach.py::cancel_decision` guards `if locked is not None:` around `record_reconciliation`+commit (inherited from `/reconcile`). Near-impossible trigger (decisions are never deleted in this app), but for a state-mutating cancel the divergence is more consequential than for a read reconcile.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-resting-order-lifecycle.md`
  summary: `record_reconciliation` advance-only guard blocks `partial`→`pending`/`timeout` but not `partial`→smaller-qty `partial`; a stale/racing smaller partial read could overwrite a larger one. Add a filled_qty-monotonic guard to fully honor the spec's "larger partial" wording.
  evidence: `coach/decision_record.py::record_reconciliation` checks only `outcome.status in (pending, timeout)` for a partial; it does not compare `filled_qty`. Near-zero reachability because broker fills are monotonic, but the guard's intent ("advance to filled or a larger partial") is not fully enforced.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-resting-order-lifecycle.md`
  summary: The `tests/test_coach_api.py` integration suite (108 tests) is prohibitively slow — the file exceeds the 600s pytest timeout when run whole, forcing every dev-auto review pass to split it into ~46-node batches at ~4–5 min each. This test wall-time, not agent reasoning, dominated Story 8.2's ~2h50m run (the review×2 pass spent ~40 min purely waiting on pytest). Speed up or mark/segregate the slow integration tests so review cycles don't pay a ~15-min tax each pass.
  evidence: Observed live during 8.2 review×2: `uv run pytest tests/test_coach_api.py -q` returned exit code 143 (timeout) at 600s; the agent then ran `--collect-only` (108 tests), split into batches, and each batch took ~272s+ (`45 passed ... in 272.68s`). Not a correctness issue — the suite passes when batched — but a compounding productivity/cost drag on the autonomous loop (every review re-runs the full suite). Likely causes: real FastAPI TestClient app startup + per-test DB/session setup with no shared fixtures, and integration tests that could be unit-level. Revisit as a test-performance pass: introduce session-scoped app/DB fixtures, mark the heaviest as `@pytest.mark.slow` and exclude from the default review run, or shard by module so no single file blows the timeout.
  resolution: RESOLVED by Story 8.5 (2026-08-04). Root cause was three stacking costs, addressed test-only (no product change): (1) per-register/login Argon2 (~22ms each ×2 per test ×~100 tests) → a test-only fast SHA-256 pwdlib hasher injected via `tests/conftest.py` monkeypatching `UserManager.__init__`'s `None` password_helper default (production `api/users.py` untouched, still Argon2; `test_register.py` opts out via the new `real_hasher` marker to keep asserting real hashing); (2) `create_app()` rebuilt per test → session-scoped shared app+TestClient in conftest with an autouse `dependency_overrides` isolation guard; (3) autouse per-test `ensure_tables` DDL storm → moved to a session-scoped one-time fixture in conftest (every DDL statement preserved verbatim). Result: `test_coach_api.py` 26.4s→15.3s; full backend suite ~50s→~35s; the whole default lane now runs in ONE `uv run pytest -q` invocation (~35s, well under the 600s timeout) — no batching, no slow-lane split needed. Same 591 node IDs collected pre/post (zero tests deleted/weakened/reclassified); stable twice-in-a-row. The `slow` marker is registered for future use but nothing is tagged today.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-order-entry-ui-beginner-warnings.md`
  summary: The Decisions SPA re-derives effective-outcome-status precedence (reconciliation over cosign) client-side; a future improvement would add a server-computed `effective_outcome_status` to `DecisionDetailResponse` so the "working/cancellable" gate can't drift from the authoritative backend `effective_outcome_status`.
  evidence: `DecisionDetailResponse` (ballast/backend/api/coach.py) ships only raw snapshots, unlike the list payload which already exposes `outcome_status`; the frontend duplicates the backend `effective_outcome_status` precedence in `Decisions.jsx`. Out of Story 8.3's pure-frontend, no-backend-change scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-order-entry-ui-beginner-warnings.md`
  summary: In the Decisions view, a `reloadKey` bump after a confirmed cancel resets the cancel lifecycle state; if a reconcile read still lags at `pending`, the "Working" label + Cancel control could momentarily re-appear after an honest "Cancelled".
  evidence: The detail effect resets cancel state on `[selectedId, reloadKey]`; `cancelledStatus` is cleared on any reloadKey bump. Narrow — does not fire in the normal cancel flow (nothing bumps reloadKey immediately after a cancel), so deferred rather than patched.

## Deferred from: follow-up review of spec-8-3-order-entry-ui-beginner-warnings (2026-08-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-order-entry-ui-beginner-warnings.md`
  summary: The pre-existing frontend `readDetail` helper (in `CoachConsult.jsx`, unchanged since Story 4.11) reads a backend error message via `data?.detail ?? data?.message`, but the app-wide error envelope is `{error:{type,message}}` (`api/app.py::_error_response`), so the APPROVE path's calm 409/422 reason is never read from `error.message` and silently falls back to hardcoded copy — the same envelope mismatch just patched on the NEW 8.3 cancel path, but on the older, out-of-scope approve path.
  evidence: `ballast/frontend/src/components/CoachConsult.jsx::readDetail` returns `data?.detail ?? data?.message ?? ''`; every backend `HTTPException` is reshaped by `api/app.py`'s handlers into `{"error":{"type":...,"message":...}}` (no top-level `detail`/`message`), so `readDetail` evaluates to `''` on a real 409/422 and the approve UI shows its fallback strings, not the backend's verbatim reason. Present since Story 4.11 (verified at the 8.3 baseline `0a7616b`; the 8.3 diff does not touch `readDetail`), and the pre-8.3 approve tests stub the same fictional `{detail}` shape, so the suite is green against a contract the backend does not implement. Not caused by Story 8.3 (its cancel-path twin was patched this pass) — logged for a focused envelope-unification pass: make `readDetail` (and any other `.detail` readers) prefer `data?.error?.message`, and re-stub the approve error tests to the real `{error:{message}}` envelope so they validate the true contract.

## Deferred from: review of spec-8-4-ai-suggest-populate-order (2026-08-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-ai-suggest-populate-order.md`
  summary: The new `BrokerPort.get_quote` (suggest-order's live-ask read) has no calm timeout/retry envelope like the placement path, so a slow live Schwab quote endpoint could block the request and surface a non-calm failure instead of a 422/409 envelope.
  evidence: `brokers/schwab_adapter/adapter.py::get_quote` calls `client.get_quote(symbol)` synchronously inside the async method with no timeout wrapper, unlike `place_order`'s calm-timeout handling. No current impact — live Schwab is gated by the Story 7.7 real-money gate and untested (the fake adapter returns a deterministic quote); fold the quote read into the same live-path timeout/retry hardening before any live suggest-order use.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-ai-suggest-populate-order.md`
  summary: The "recent 20-day low" uses the 20 most-recent `market_daily` bars irrespective of their `day`, with no data-freshness guard, so stale ingestion yields a stale suggested resting price while the narration confidently says "today's ask" / "recent low".
  evidence: `coach/suggest.py::_recent_low` orders by `day desc` limit 20 but never checks the newest bar's recency; the same unguarded-staleness pattern exists on other `MarketDaily` consumers (e.g. `precedent/engine.py::_load_series`). Broader than Story 8.4 (a cross-cutting market-data freshness concern) — revisit with a shared "bars are fresh enough" guard or a surfaced as-of date on any price-derived suggestion/precedent.

## Deferred from: follow-up review of spec-8-4-ai-suggest-populate-order (2026-08-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-ai-suggest-populate-order.md`
  summary: The suggested dollar `amount`/`shares` shown on the form and in the reasoning panel can diverge from the amount actually co-signed, because the co-sign base is `recommendation.order_intent ?? submitted.order` and Story 8.3 makes the coach's LLM-returned `order_intent.amount` authoritative for {symbol, side, amount} — the P1 fix deliberately re-seeds only the resting LIMIT price + GTC, never the amount.
  evidence: `CoachConsult.jsx` populates the form via `setAmount(data.amount)` and holds only `{limit_price, duration}` in `pendingSuggestion`; at approve, `buildOrderIntent(orderIntent, options)` (orderOptions.js) takes `amount` from the base, and `recommendation.order_intent.amount` is parsed straight from the LLM output (`coach/recommendation.py:190-194`). When the coach returns an order_intent whose amount differs from the suggested whole-share cost, the co-signed order carries the coach's amount + the suggested LIMIT price/GTC — a share count the suggest engine never sized — while the reasoning panel still states the suggested "$X total cost". Not a clean patch: reconciling "which sizing wins" is a product decision touching the locked 8.3 amount-authority model. Revisit whether the suggested sizing should override the coach's amount at co-sign, or reconcile the displayed narration to the co-signed amount.

## Deferred from: implementation of spec-8-5-test-suite-performance (2026-08-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-5-test-suite-performance.md`
  summary: 4 pre-existing test failures (`test_llm_gateway.py::test_anthropic_adapter_no_key_raises_without_importing_sdk`, `::test_factory_anthropic_no_key_raises`, `::test_factory_returns_fake_by_default`, `test_market_ingest.py::test_factory_returns_fake_by_default`) fail on the developer's local machine because the checked-out `.env` sets the demo config (`LLM_ADAPTER=anthropic` + a real `ANTHROPIC_API_KEY`, `MARKETDATA_ADAPTER`/etc.), which the settings object loads at import — so `monkeypatch.delenv("LLM_ADAPTER")` inside these tests cannot force the factory back to the fake default, and the "no-key raises" tests see a real key. Verified NOT caused by Story 8.5: they fail identically on the untouched baseline commit `8573332` with the 8.5 changes stashed. Outside Story 8.5's scope (`test_coach_api.py` performance only) and outside the files it may touch.
  evidence: These tests `monkeypatch.delenv(...)` / expect a missing key and assert the factory returns the fake adapter, but pydantic-settings reads the on-disk `.env` (demo config, per project memory `fake-mode-vs-real-coaching`) at settings construction, so `get_llm_gateway()` resolves to `AnthropicGateway`. They are environment-dependent, not a code regression: presumably green in CI / whenever the loop runs with `LLM_ADAPTER=fake` and no `.env` key. Revisit as a test-hardening pass unrelated to 8.5: make these factory tests robust to a populated local `.env` (e.g. construct a fresh `Settings(_env_file=None)` or patch the cached settings singleton rather than only `os.environ`), or document that the local `.env` demo config makes them expected-red locally. Do NOT "fix" by deleting/weakening; the assertions are legitimate.

## Deferred from: review of spec-8-5-test-suite-performance (2026-08-05, reopened fix)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-5-test-suite-performance.md`
  summary: There is no test-level timeout guard, so a future re-introduction of `scope="session"` on the async `ensure_tables`/`client` fixtures (the exact bug that got 8.5 reopened) would silently HANG the suite at ~0% CPU forever instead of failing loudly; both adversarial reviewers converged on adding one.
  evidence: `pyproject.toml [tool.pytest.ini_options]` has no `addopts` timeout and `pytest-timeout` is not a dependency; the reopen root cause was a silent cross-event-loop asyncpg stall with no CPU and no failure. A per-test timeout (e.g. `pytest-timeout` with `--timeout=60`) would convert this whole bug class from "hangs CI indefinitely" to a fast, obvious failure. Deliberately NOT done in this fix pass to keep the reopened change minimal and avoid adding a new test dependency mid-fix; revisit as a small test-hardening story.
  summary: `ensure_tables` is `autouse=True` in the shared conftest, so every test in every non-overriding module now opens `db.session.engine` and runs ~8 idempotent DDL statements before it — forcing a running Postgres even for tests that touch no DB and adding a small per-test cost across the whole suite (measured negligible today: full lane ~46s).
  evidence: `tests/conftest.py::ensure_tables` (autouse, function-scoped) runs unconditionally; ~5 modules (`test_suggest_order.py`, `test_atomic.py`, `test_recoverable_placement.py`, `test_portfolio.py`, `test_digest_job.py`) define their own name-shadowing local `ensure_tables` (so they don't double-run, but the duplication is now redundant). Pre-existing (introduced when conftest was first added, baseline `e6b217c`), not caused by the scope-revert diff. Revisit: scope the autouse DDL to only the DB-integration files that need it and delete the redundant local copies, or gate it so pure-unit tests don't require Postgres.

## Deferred from: code review of 9-1-cash-state-model-reserve-parked-funds (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/9-1-cash-state-model-reserve-parked-funds.md`
  summary: The `cash_config.parked_symbols` JSON column is not `MutableList.as_mutable`-tracked.
  evidence: `ballast/backend/db/models.py` declares `parked_symbols: Mapped[list] = mapped_column(JSON, ...)`. Safe today because the only writer, `cash/config.py:set_parked_symbols`, always REASSIGNS the whole attribute (`config.parked_symbols = normalize_symbols(...)`), which triggers ORM dirty-tracking. Latent trap: any future code that mutates the list in place (`config.parked_symbols.append(...)`) will silently not persist. Fix when touched: wrap with `MutableList.as_mutable(JSON)` or add an explicit "reassign-only" comment.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-2-honest-yield-aware-missed-growth.md`
  summary: The disclosed money-market APY percentage is formatted independently on the backend (Decimal.quantize(0.1)) and the frontend (Number.toLocaleString maxFrac:1), so a future non-round tuned DEFAULT_MONEY_MARKET_APY could render two different percentages in the statement vs. the yield note.
  evidence: Both render "4%" while the constant is 0.04, but e.g. 0.0425 → backend "4.2" (half-even on 4.25) vs frontend "4.3"; pairs with the existing Epic 8 action item to tune placeholder constants against real data. Fix by having the backend emit a single pre-formatted apy percentage string the frontend renders verbatim.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-3-just-in-time-liquidation-deferred-buy.md`
  summary: Multiple concurrent `awaiting_funds` pending buys each compute `funds_ready`/guard resume against the FULL `ready_to_trade` cash in isolation, so two buys can both read ready against cash that only covers one (aggregate over-draw of the optimistic UI signal).
  evidence: `ballast/backend/api/cash.py::_pending_buy_out` sets `funds_ready = ready_to_trade >= pending.amount` and `resume_pending_buy` guards `view.cash < pending.amount` per-row with no cross-pending-buy accounting. Broker-backstopped (the `/approve` whole-share sizing off live ask + insufficient-funds rejection is authoritative), so no real money over-draws — but the card can optimistically show two pending buys as resumable at once. Fix (needs product thought on ordering/capping): subtract the sum of OTHER open pending-buy amounts before the compare, or cap to one active pending buy per owner; add a two-pending-buy test.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-3-just-in-time-liquidation-deferred-buy.md`
  summary: On dedupe, `/liquidation-plan` returns a freshly-computed `sell_order_intent` paired with the STALE linked `sell_decision_id`, so if cash changed between visits the card's sell amount drifts from the proposed SELL decision's immutable snapshot.
  evidence: `ballast/backend/api/cash.py::liquidation_plan` (dedupe branch) sets `sell_decision_id = existing.sell_decision_id` but `_plan_response` returns `plan.sell_order_intent` recomputed from current cash/holdings; `/approve` places `body.order_intent` verbatim (no reconciliation vs `recommendation_snapshot`, confirmed `api/coach.py`). Consequence is Decisions-history/replay cosmetic (the placed order still matches what the user saw on the card), not a money error. Fix: on dedupe, re-mint/replace the SELL decision when the computed amount differs, or return the intent recorded on the linked decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-3-just-in-time-liquidation-deferred-buy.md`
  summary: `/api/coach/approve` trusts `order_intent` verbatim (no reconciliation against the decision's `recommendation_snapshot`); the Story 9-3 SELL scope-gate widening ENLARGES what a repurposed decision_id can place — a user can co-sign any of their own proposed decisions while submitting an arbitrary-amount SELL of their own declared parked fund.
  evidence: `ballast/backend/coach/execution.py::execute_approved_order` widens SELL scope to the scope user's `parked_symbols` (correctly per-owner, fail-closed on absent scope — no cross-user exposure), but combined with the pre-existing verbatim-trust at `api/coach.py::approve` (decision_id used only for the atomic claim/idempotency key) the blast radius is now "any amount SELL of the user's own parked fund." Bounded to the user's own money/own declared funds — not privilege escalation — but the durable fix is to reconcile `body.order_intent` against the decision snapshot before placing. Pre-existing gap enlarged by this story; flag before real-money reliance widens further.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-3-just-in-time-liquidation-deferred-buy.md`
  summary: The idempotent-dedupe read in `POST /liquidation-plan` is a non-atomic SELECT with no DB uniqueness constraint, so two concurrent plan requests for the same `(owner, symbol, amount)` can both miss and each mint a duplicate `awaiting_funds` PendingBuy + a duplicate proposed SELL decision.
  evidence: `ballast/backend/api/cash.py::_find_awaiting_pending_buy` is a plain scoped SELECT and `liquidation_plan` inserts on the None branch with no `SELECT ... FOR UPDATE` and no unique index on `(owner_id, status, symbol, amount)`; a concurrent double-submit (plausible given the auto-firing `useEffect` in `CoachConsult.jsx`) TOCTOU-races into two rows. Distinct from the existing aggregate-over-draw entry (which is about resume-time cash checks across pending buys); this one is duplicate creation of the SAME buy. Broker-backstopped at `/approve` placement (nothing auto-places), so no money error — but duplicate cards/decisions confuse the user. Fix: add a partial unique index on `(owner_id, symbol, amount) WHERE status='awaiting_funds'` (both `create_all` + the carried-over-DB migration) or a `SELECT ... FOR UPDATE` dedupe; add a concurrency test.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-3-just-in-time-liquidation-deferred-buy.md`
  summary: `POST /liquidation-plan` is a state-mutating endpoint (mints a durable PendingBuy + a proposed SELL decision) but is invoked automatically from a presentation `useEffect` in `CoachConsult.jsx`, so merely rendering the buy-approve step while short on cash writes DB rows before the user expresses any liquidation/defer intent.
  evidence: `ballast/frontend/src/components/CoachConsult.jsx` fires `POST /api/cash/liquidation-plan` inside a `useEffect` keyed on `[phase, decision_id, symbol, amount, side]` with no user gesture; the endpoint durably inserts a PendingBuy and a proposed SELL decision (`api/cash.py`). Server-side dedupe stops duplicate *rows* on re-render, but a read-shaped effect issuing a mutating POST is fragile (pairs with the concurrent-dedupe TOCTOU above) and surprising for a beginner (a "proposed sell" decision + a pending buy appear from viewing alone). Fix: guard the POST behind a one-shot ref per decision_id, and/or split a read-only `preview` plan from the durable `commit` that mints rows only on explicit user intent.

- source_spec: `_bmad-output/implementation-artifacts/spec-9-3-just-in-time-liquidation-deferred-buy.md`
  summary: Both new pending-buy reads (`GET /pending-buys` and the plan-time dedupe lookup) call `repo.list()`, which materializes ALL of the owner's pending_buy rows — including every `resumed`/`cancelled` row ever — into memory and filters `status=='awaiting_funds'` in Python, so the `(owner_id, status)` index the spec added specifically to back this read is never used and the read grows unbounded (pending_buy is append-only, never pruned).
  evidence: `ballast/backend/api/cash.py::list_pending_buys` (line 544) and `::_find_awaiting_pending_buy` (line 404) both `await repo.list()`, which issues `SELECT * WHERE owner_id = ?` with no status predicate (`db/repository.py:74`) and loads every row before the Python-side `status` filter; `ScopedRepository.list_page` (`db/repository.py:85`) exists precisely for SQL-side filtered/paged reads (Story 6.6) and is not used, and the `Index("...", "owner_id", "status")` on `PendingBuy` (`db/models.py`) cannot serve a full-table `SELECT *`. Low consequence at current beginner single-user scale (few rows), but a real coherence gap in this story's own work (added an index the read can't use) and unbounded over time. Same class as the already-logged 6.6/4.9 "unbounded per-user table + index that the read doesn't exploit" entries. Fix: switch both reads to `list_page` with a SQL-side `status='awaiting_funds'` filter so the index is used and resumed/cancelled history is never loaded.

- source_spec: `_bmad-output/implementation-artifacts/spec-10-3-fiduciary-advisor-narration-never-invent-safeguard.md`
  summary: The new `GET /api/allocation/narration` handler runs the synchronous blocking `gateway.complete()` (a real Anthropic network round-trip on the non-fake path) directly inside an `async def` endpoint, stalling the event loop for the call's duration.
  evidence: `ballast/backend/api/allocation.py::read_narration` calls `narrate_plan(get_llm_gateway(), plan)` → `gateway.complete()` (`llm/anthropic_adapter.py`) without `await asyncio.to_thread(...)`. This is NOT a regression introduced by 10-3 — it exactly mirrors the pre-existing `coach/suggest.py::suggest_resting_order` → `narrate_suggestion` pattern already in the codebase, so it is a repo-wide habit worth a single focused fix (offload every synchronous `gateway.complete` via `asyncio.to_thread`), not a 10-3-local defect. Low consequence today (fake adapter is instant; single-user beginner scale), real under live-LLM concurrency.
